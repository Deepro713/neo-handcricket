---
name: Bug report
about: Something is broken or behaving unexpectedly
title: '[bug] '
labels: bug
assignees: ''
---

## What happened

A clear, brief description of the bug.

## Steps to reproduce

1. Run `python -m neo_handcricket`
2. Pick country: `...`
3. Pick opponent: `...`
4. Format: `T20` / `Test` / `Custom (...)`
5. Difficulty: `easy` / `medium` / `hard`
6. (the action that triggered the bug)

## What I expected

What should have happened.

## What actually happened

What did happen — error message, stack trace, or unexpected behaviour.

```
(paste any traceback or terminal output here)
```

## Environment

- OS: (macOS 14, Ubuntu 22.04, WSL Ubuntu, etc.)
- Python version: `python3 --version`
- Terminal: (Terminal.app, iTerm2, Alacritty, gnome-terminal, …)
- neo-handcricket commit: `git rev-parse --short HEAD`

## Save file

If a saved match is involved, attach the relevant `saves/*.json` (with any personal info redacted).

## Notes

Anything else that might help — recent code changes, custom roster edits, anything unusual.
